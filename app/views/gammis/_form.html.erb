<%= form_for(@gammi) do |f| %>
  <% if @gammi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gammi.errors.count, "error") %> prohibited this gammi from being saved:</h2>

      <ul>
      <% @gammi.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
